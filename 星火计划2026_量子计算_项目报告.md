# 腾讯星火计划 2026 · 量子计算赛道 — 项目报告

> **赛题**：Tencent Sparking Program 2026: Quantum Computing  
> **主办**：腾讯量子实验室  
> **选题**：Problem 1 + Problem 2（SQD & EWF 嵌入波函数）  
> **代码仓库**：`quantumnyloong`

---

## 目录

1. [项目概述](#1-项目概述)
2. [Problem 1：量子计算基础预备与热身](#2-problem-1量子计算基础预备与热身)
   - [1.1–1.3 旋转算符与 U(2) 参数化](#11--13-旋转算符与-u2-参数化)
   - [1.4 量子态制备](#14-量子态制备)
   - [1.5 门构造](#15-门构造)
   - [1.6 测量](#16-测量)
   - [1.7 可逆经典电路](#17-可逆经典电路)
   - [1.8 Grover 搜索算法](#18-grover-搜索算法)
3. [Problem 2：SQD 与 EWF 嵌入波函数](#3-problem-2sqd-与-ewf-嵌入波函数)
   - [2.1 哈密顿量构建与映射](#21-哈密顿量构建与映射)
   - [2.2 子空间维数与组态恢复](#22-子空间维数与组态恢复)
   - [2.3 LUCJ 电路与门分解](#23-lucj-电路与门分解)
   - [2.4 HF-SQD vs LUCJ-SQD](#24-hf-sqd-vs-lucj-sqd)
   - [2.5 SQD vs VQE 与混合方案](#25-sqd-vs-vqe-与混合方案)
   - [2.6 FCI 指数爆炸与 EWF O(N) 标度](#26-fci-指数爆炸与-ewf-on-标度)
   - [2.7 DMET 分片与 Schmidt 分解](#27-dmet-分片与-schmidt-分解)
   - [2.8 能量重构与双计数消除](#28-能量重构与双计数消除)
   - [2.9 完整 EWF-SQD 管道与误差分析](#29-完整-ewf-sqd-管道与误差分析)
4. [可选/进阶工作](#4-可选进阶工作)
   - [最优 ccsd_scale 扫描](#41-最优-ccsd_scale-扫描)
   - [多层 DF-LUCJ](#42-多层-df-lucj)
   - [纠缠熵与采样需求](#43-纠缠熵与采样需求)
   - [醋酸分子 SQD 实验](#44-醋酸分子-sqd-实验)
   - [Optional1：ccsd_scale 深度研究](#45-optional1ccsd_scale-深度研究)
5. [基础设施与工具链](#5-基础设施与工具链)
6. [关键发现总结](#6-关键发现总结)
7. [项目文件索引](#7-项目文件索引)

---

## 1. 项目概述

本项目围绕量子计算化学中的**基于采样的量子对角化（SQD）**方法与**嵌入波函数（EWF）**理论展开，从量子计算基础概念到完整的分子能量计算管道，实现了端到端的理论与工程闭环。

**技术栈**：PySCF, TensorCircuit, ffsim, Qiskit, qiskit-addon-sqd, NumPy, SciPy, LaTeX

**测试分子**：

| 分子 | 化学式 | 基组 | 量子比特数 | FCI 行列式数 |
|------|--------|------|-----------|-------------|
| Hydrogen | H₂ | STO-3G | 4 | 6 |
| Lithium Hydride | LiH | STO-3G | 8 | 70 |
| Water | H₂O | STO-3G | 14 | 1,001 |
| Nitrogen | N₂ | STO-3G | 20 | 38,760 |
| Ethylene | C₂H₄ | STO-3G | 28 | 30,421,755 |

---

## 2. Problem 1：量子计算基础预备与热身

### 1.1 – 1.3 旋转算符与 U(2) 参数化

**核心推导**：

1. **旋转算符显式形式**：证明 $R_{\mathbf{n}}(\theta) = e^{-i\frac{\theta}{2}\mathbf{n}\cdot\boldsymbol{\sigma}} = \cos(\theta/2)I - i\sin(\theta/2)(n_X X + n_Y Y + n_Z Z)$。关键步骤是利用 Pauli 矩阵反对易关系导出 $(\mathbf{n}\cdot\boldsymbol{\sigma})^2 = I$，将指数 Taylor 展开按奇偶项分组。

2. **U(2) 参数化**：证明任意 $U \in U(2)$ 可写为 $e^{i\alpha} R_{\mathbf{n}}(\theta)$。提取全局相位后约束于 SU(2)，对比一般形式 $\begin{pmatrix} a & -b^* \\ b & a^* \end{pmatrix}$ 直接解出参数。

3. **Euler 角分解**：
   $$R_{\mathbf{n}}(\theta) = R_Z(\alpha)R_Y(\beta)R_Z(\theta)R_Y(-\beta)R_Z(-\alpha)$$
   其中 $\mathbf{n} = (\sin\beta\cos\alpha, \sin\beta\sin\alpha, \cos\beta)$。

### 1.4 量子态制备

完成了五种量子态的精确制备方案与最优性分析：

| 态 | 线路 | 门复杂度 |
|----|------|---------|
| (a) 均匀叠加态 $\frac{1}{\sqrt{2^n}}\sum_x \vert x\rangle$ | $H^{\otimes n}$ | n 个 H 门（最优） |
| (b) GHZ 型态 $\alpha\vert 0^n\rangle + \sqrt{1-\alpha^2}\vert 1^n\rangle$ | $R_Y(\theta)$ + (n-1) CNOT | 1 旋转 + n-1 CNOT |
| (c) 4 比特加权叠加态 | 1 ancilla + 控制旋转级联 | 1 ancilla + 5 CNOT + 2 CRy |
| (d) 7 比特 CSS 编码态 ⭐ | CSS 码结构 + 逻辑旋转 | 27 门（紧下界） |

**1.4(d) CSS 编码态——项目亮点**：

态具有 CSS 编码结构 $|\psi\rangle = \alpha|0_L\rangle + \beta|1_L\rangle$，其中：
- $\alpha$ 组 = $\vec{a} + C$，$\vec{a}=1000000$，$C = \text{span}\{1010101, 0110011, 0001111\}$
- $\beta$ 组 = $\alpha$ 组的逐位取反（$X_L = X^{\otimes 7}$）

**最优线路（27 门，紧下界证明）**：
- 编码 $|0_L\rangle$：12 门（3H + 8CNOT + 1X），Tanner 图边着色优化
- 逻辑旋转：15 门（11H + 7$R_Z$，合并优化）
- 比通用态制备（~128 CNOT）压缩 **16 倍**

下界证明：
- CNOT ≥ 8（Tanner 图 9 边，链式节省 1）
- H ≥ 11（创建 16 项叠加需 4 个，逻辑旋转需 7 个）
- $R_Z$ ≥ 7（7 独立比特，不可合并）
- X ≥ 1（仿射偏移）
- **总计 27 门为紧下界**

### 1.5 门构造

系统构造了六种量子门的分解方案：

| 门 | 分解 | 资源 |
|----|------|------|
| SWAP | 3 CNOT（$C_{0\to 1} C_{1\to 0} C_{0\to 1}$） | 3 CNOT |
| 最近邻 CNOT 路由 | $C_{0\to 2}$ 和 $C_{1\to 3}$ | 6 CNOT |
| $R_{ZZ}(\theta)$ | $C_{0\to 1} \cdot [I\otimes R_Z] \cdot C_{0\to 1}$ | 2 CNOT + 1 RZ |
| CPhase | $R_Z\otimes R_Z \cdot R_{ZZ}(-\theta/2)$ | 2 CNOT + 3 RZ |
| $e^{-i\theta P}$（任意 n 比特 Pauli） | 基旋转 + CNOT 链 + $R_Z(2\theta)$ + 逆序撤销 | 2(m-1) CNOT + 1 RZ |
| SU(4) 分解 | Cartan (KAK) 分解 | 5 CZ + 17 单比特 |

### 1.6 测量

实现并分析了六种测量线路：

| 测量任务 | 方法 | 关键结果 |
|---------|------|---------|
| (a) $Z_1Z_2$ 和 $X_1X_2$ 共同 +1 投影 | Bell 基测量 / 非破坏稳定子测量 | 后选择成功率 $|\langle\Phi^+|\psi\rangle|^2$ |
| (b) 任意 Pauli 算符投影 | $U_i$ → CNOT 奇偶校验 → 辅助测量 → $U_i^\dagger$ 复原 | 通用模板 |
| (c) GHZ → 单比特压缩 | H 作用于 q₂⋯qₙ，奇偶后选择，$Z^s$ 前馈 | 确定性成功（概率 1） |
| (d) 5 比特扇出门 $F_4$ | 阶梯结构 vs 分治结构 | **Pareto 前沿**：{(7,7), (10,6)} |
| (e) Swap Test | $H \to \text{cSWAP} \to H$ → 测概率 | $P(0)=\frac12(1+\vert\langle\psi|\phi\rangle\vert^2)$ |

**1.6(d) Pareto 前沿**——通过 BFS 穷举证明了深度 ≤ 5 不可达，确定了最近邻限制下扇出网络的最优门数与深度的不可兼得边界。

### 1.7 可逆经典电路

用 Toffoli + NOT 门构造计算 $S_3(x)$ 的可逆电路。

**布尔函数简化**：通过真值表分析，$S_3(x)$ 简化为唯一极小项：
$$S_3(x) = \neg x_1 \land \neg x_2 \land x_3$$
仅当输入为 $|001\rangle$ 时为真。

**电路实现**（Compute-Uncompute 模式）：
```
X(x1), X(x2)                           # 翻转控制位
CCNOT(x1, x2 → anc)                    # anc = ¬x1 ∧ ¬x2
CCNOT(anc, x3 → result)                # result = anc ∧ x3
CCNOT(x1, x2 → anc)                    # Uncompute anc
X(x1), X(x2)                           # Uncompute inputs
```

**资源**：7 门 × 5 量子比特 = 35

**验证**：Python 脚本枚举全部 8 种输入，验证输出正确性；生成 Qiskit 电路图 PDF。

### 1.8 Grover 搜索算法

基于第 7 题的可逆电路构造 Oracle，搜索满足 $S_3(x) = \text{True}$ 的输入。

**参数**：
- 搜索空间 $N = 8$，解数 $M = 1$
- $\theta = \arcsin(\sqrt{1/8}) \approx 0.361$ rad
- 最优迭代 $k_{opt} = \lfloor \arccos(\sqrt{1/8}) / (2\theta) \rfloor$

**Oracle 构造**：Compute-Uncompute 模式（$S_3 \to Z \to S_3^\dagger$），辅助比特全部归零。

**Diffusion 算符**：$H^{\otimes 3} \cdot X^{\otimes 3} \cdot \text{MCZ} \cdot X^{\otimes 3} \cdot H^{\otimes 3}$，MCZ 通过 Toffoli + H 实现。

**验证**：扫描 $k=0$ 至 $k_{opt}+1$，追踪解态概率，确认最优迭代步高概率命中 $|001\rangle$。

---

## 3. Problem 2：SQD 与 EWF 嵌入波函数

### 2.1 哈密顿量构建与映射

针对 H₂ 和 N₂，完成了三种费米子-量子比特映射的对比分析：

| 映射 | H₂ 最大权重 | N₂ 最大权重 | 特点 |
|------|-----------|-----------|------|
| Jordan-Wigner (JW) | 4 | 20 | 简单直观，Pauli 权重 O(n) |
| Parity | 2 | 18 | 内置 2 量子比特对称性约化 |
| Bravyi-Kitaev (BK) | 4 | **13** | O(log n) 权重增长 |

**Z₂ 对称性约化**：对 H₂ 利用稳定子 $-Z_0Z_2$ 和 $-Z_1Z_3$，通过 `taper_off_qubits` 将 4 量子比特 → 2 量子比特，大幅降低计算开销。

### 2.2 子空间维数与组态恢复

**子空间维数分析**（LiH, STO-3G）：
- 从 HF 态出发：$M = 1$（单一行列式）
- 从 UCCSD ansatz 出发：$M$ 可达 36（含多种激发组态）

**组态恢复算法**：
- 实现贪心最大似然恢复（`max_dev` 策略）：对违反电子数的比特串，反复翻转 $|b_i - \bar{n}_i|$ 最大的位
- 噪声扫描实验（p = 0.10–0.50）：找到失效阈值 ≈ 33%
- 从最大似然角度证明了贪心策略的最优性

### 2.3 LUCJ 电路与门分解

**LUCJ ansatz 结构**：
$$|\Psi_{\text{LUCJ}}\rangle = U \, e^{iJ} \, U^\dagger \, |\text{HF}\rangle$$

其中：
- $U$：由 CCSD $t_1$ 振幅通过 QR 式 Givens 消除构造的轨道旋转
- $e^{iJ}$：对角库仑 Jastrow 因子，$J = \sum_{ij} J_{ij} n_i n_j$
- 双激发 Givens 旋转：产生成对双激发概率幅度

**门分解**：

| 门 | 分解 | CNOT 数 |
|----|------|---------|
| Givens 旋转 $G(\theta)$ | 2 CNOT + 4 单比特 | 2 |
| Jastrow 对 $e^{iJ_{ij} n_i n_j}$ | $RZ_i \cdot RZ_j \cdot RZZ_{ij}$ | 2 |
| RZZ 门 | CNOT 对 + $R_Z$ | 2 |

**关键物理洞察**：闭壳层下 CCSD $t_1 \approx 0$（Brillouin 定理），$U \approx I$。$e^{iJ}$ 在 HF 数本征态上仅贡献相位。真正产生比特串多样性的唯一途径是**成对 Givens 旋转**（alpha 和 beta 自旋同时施加）。

### 2.4 HF-SQD vs LUCJ-SQD

在 LiH（STO-3G, CAS(4,(1,1))）上对比了两种方法。

**核心发现**：在 λ = 0.05，S = 1000 条件下，LUCJ-SQD 与 HF-SQD 给出**相同能量**（$E = -7.86307516$ Ha）。

**原因分析**：
- 双激发概率 $p_{\text{double}} \approx 1.25 \times 10^{-4}$
- 阈值 $1/S = 10^{-3}$
- $p_{\text{double}} < 1/S$，意味着 1000 次采样几乎采不到双激发组态
- LUCJ-SQD 退化为 HF-SQD

**结论**：λ 必须足够大才能让双激发组态概率超过采样阈值。覆盖条件：$S \gtrsim 3 / p_{\text{double}}$。

### 2.5 SQD vs VQE 与混合方案

**资源对比**：

| 维度 | SQD | VQE |
|------|-----|-----|
| 量子电路执行次数 | $O(S)$ | $O(pN^4/\varepsilon^2)$ |
| 经典后处理 | 子空间对角化 $O(M^3)$ | 经典优化器 |
| 噪声韧性 | 强（后处理筛选） | 弱（直接能量期望值） |
| 态制备复杂度 | 低（LUCJ, O(N²) CNOT） | 高（UCCSD, O(N⁴) CNOT） |

**混合方案（进阶挑战）**：验证了三明治不等式：
$$E_0 \leq E_{\text{hybrid}} \leq E_{\text{VQE}}|_{S}$$

即：从 VQE 态采样后进行 SQD 子空间对角化得到的能量，严格介于精确 FCI 与 VQE 投影能量之间。在 LiH 上，$n_{\text{shots}}$ 从 5 到 2560 全程满足该不等式。

### 2.6 FCI 指数爆炸与 EWF O(N) 标度

**FCI 维数增长**（STO-3G 基组）：

```
H₂   : C(4,2)  = 6
LiH  : C(8,4)  = 70
H₂O  : C(14,10)= 1,001
N₂   : C(20,14)= 38,760          ← 经典可行边界
C₂H₄ : C(28,16)= 30,421,755      ← 经典不可行(~3×10⁷)
```

**EWF 标度优势**：
- 直接 FCI：$O\left[\binom{2N}{N_e}^3\right]$ — 指数爆炸
- EWF + 分片：$F \cdot O\left[\binom{2n_{\text{frag}}}{N_{\text{frag}}}^3\right] = O(N)$ — 线性标度
- 推导交叉点条件：只要 $n_{\text{frag}} < N$，EWF 代价严格小于直接 FCI
- C₂H₄ 在 NISQ 约束下（每碎片 ≤ 20 量子比特）需 2 个碎片

### 2.7 DMET 分片与 Schmidt 分解

**核心定理**：DMET minimal bath 在 HF 级解下是精确的——单粒子密度矩阵 $\gamma$ 的投影子性质保证 Schmidt 分解与 HF 波函数的等价性。当关联求解器作用于嵌入哈密顿量时，该精确性不再保持，但提供良好的初始近似。

**实现要点**：
- Löwdin 正交化：$P = S^{1/2} C_{\text{occ}} C_{\text{occ}}^T S^{1/2}$
- Bloch 基变换 + SVD bath 轨道提取
- Core 嵌入势并入 $h_{\text{emb}}$

### 2.8 能量重构与双计数消除

**核心公式**（Hartree 格式）：
$$E_{\text{total}} = E_{\text{MF}} + \sum_f \left(E_f - E_{\text{MF},f}\right)$$

其中 $E_f$ 是碎片 $f$ 的量子求解能量，$E_{\text{MF},f}$ 是碎片 $f$ 的平均场能量。验证：HF 用作碎片求解器时，$E_{\text{total}} \equiv E_{\text{MF}}$ 精确成立。

**碎片归属**：两种实现方式 — (a) DM 民主式（AO 基 + 全局 H + 杂质归属），(b) WF 投影式（团簇 H + 杂质首指标投影）。

### 2.9 完整 EWF-SQD 管道与误差分析

**五分子端到端结果**：

| 分子 | 活性空间 | FCI 可达？ | ε_frag | ε_SQD | 主导误差 |
|------|---------|-----------|--------|-------|---------|
| H₂ | (2,2) | ✓ | 0 | 0 | 无 |
| LiH | (4,(1,1)) | ✓ | 0 | 0.225 mHa | SQD(最优 λ) |
| H₂O | (7,10) | ✓ | 0 | 0 | 无 |
| N₂ | (14,14) | ✗ | 63.1 mHa | 0.097 mHa | **碎片×650** |
| C₂H₄ | (11,16) | ✗ | 31.1 mHa | 0.003 mHa | **碎片×10367** |

**木桶效应**：拉格朗日乘数法推导表明，在固定计算预算约束下，最优配置为 $\varepsilon_{\text{frag}} \approx \varepsilon_{\text{SQD}}$。实测数据显示，对于大分子，$\varepsilon_{\text{frag}}$ **严重主导**（N₂ 650 倍，C₂H₄ 10000+ 倍），即提高 SQD 精度对总误差几乎没有帮助——瓶颈在 DMET 碎片近似的 intrinsic error。

---

## 4. 可选/进阶工作

### 4.1 最优 ccsd_scale 扫描

在 LiH 上系统扫描 CCSD 振幅缩放参数 λ ∈ [0, 0.5]：

- 最优 λ* = **0.30**
- 对应 SQD 误差 = **0.225 mHa**（化学精度 1.6 mHa 的 1/7）
- 参考能量：E_HF = -7.863075 Ha, E_FCI = -7.864904 Ha, 关联能 = −1.829 mHa

### 4.2 多层 DF-LUCJ

实现 3 层双因子分解 LUCJ ansatz (g_k/κ_k 参数化 + 角度/相位缩放修正)：
- 单层 LUCJ 存在"局部截断平台"（1.587 mHa）
- 多层 ansatz 通过逐层注入双激发覆盖，消除了该平台
- λ ≥ 0.7 时**精确打到 FCI 能量**（误差 = 0.000 mHa）

### 4.3 纠缠熵与采样需求

LUCJ 态的纠缠熵随 λ 变化：
- λ = 0 → S_ent = 0 nats（纯 HF 态）
- λ = 1 → S_ent = 0.118 nats
- 所需采样数 $S_{\text{req}}$ 从 7 × 10⁵ 单调下降至 283

### 4.4 醋酸分子 SQD 实验

扩展到中等分子 CH₃COOH（32 电子，STO-3G，16 轨道活性空间）：

**误差分解**（双标尺法）：
- 以 CASCI 为标尺 → 子空间覆盖误差
- 以 CCSD(T) 全轨道为标尺 → 活性空间截断误差
- 得到完整的误差归因分析

### 4.5 Optional1：ccsd_scale 深度研究

系统研究了 LUCJ-SQD 中 λ 参数的非单调效应，揭示了几个重要现象：

1. **误差回升**：λ 增大后 SQD 误差先降后升，存在最优区间
2. **H₂ 周期性尖峰**：大 λ 下概率幅发生周期性交换，在特定 λ 处产生窄能量尖峰（解析公式 + 显式 5000-shot 采样双重验证）
3. **结构性覆盖不足**：单层 LUCJ 可能对重要 FCI 组态给出零概率，无论如何提高 shot 数都无法弥补——这是 ansatz 表达能力本身的限制
4. **噪声控制**：固定 K（子空间大小）+ 粒子数后选择是防止噪声产生虚假好结果的必要条件

**实验维度**：
- 受控实验（精确态诊断 × 有限 shot 诊断 × 受控噪声诊断）
- 覆盖饱和实验（n_reps=1 vs 3，FCI 组态召回率 ≥ 95% 条件）
- 大 λ 尖峰扫描（λ = 0–1000，解析峰检测 + 有限 shot 验证）

---

## 5. 基础设施与工具链

### sqd/ 公共库

独立封装的、可复用的 SQD-EWF 工具库，包含 15 道公共练习题的完整基础设施：

| 模块 | 功能 | 技术依赖 |
|------|------|---------|
| `chemistry.py` | RHF 积分提取、FCI/CCSD/MP2 参考解 | PySCF, openfermion |
| `mapping.py` | JW/Parity/BK 映射、Z₂ 约化、Pauli 统计 | openfermion, scipy |
| `ci.py` | **手写 Slater-Condon CI 矩阵**：对角元、单激发、双激发耦合 | numpy, scipy |
| `sqd.py` | 两种配置恢复（`max_dev` / `directed`）、α×β 笛卡尔积子空间 | numpy |
| `circuits.py` | HF 态制备、LUCJ ansatz、fermionic SWAP | TensorCircuit |
| `dmet.py` | DMET 碎片化：Löwdin 正交 + SVD bath + 嵌入势 | numpy, scipy, PySCF |
| `ewf_ref.py` | EWF 能量重构（DM 民主式 / WF 投影式） | PySCF |
| `cluster_solver.py` | 团簇求解器（小团簇全 FCI / 大团簇 LUCJ+SQD） | sqd, circuits, dmet |
| `parallel.py` | ThreadPoolExecutor 碎片/扫描并行 + BLAS 线程防超订 | concurrent.futures |
| `hardware.py` | 模拟器/真机统一接口、资源统计（QIR 解析）、安全默认 | TensorCircuit.cloud |

**关键设计原则**：
- CPU/GPU 自动后端切换（numpy/jax）
- NumPy 2.x 兼容适配
- 线程安全的随机种子管理
- 模拟器 ↔ 量子云真机的无缝切换（`dry_run=True` 安全默认）

### 代码架构

```
quantumnyloong/
├── Problem1/          # Problem 1 完整解答（LaTeX + Python）
│   ├── code/          # 可逆电路 + Grover 搜索
│   └── figs/          # 电路图
├── Problem2/          # Problem 2 完整解答（LaTeX + Python + 数据）
│   ├── code/          # 哈密顿量、LUCJ、SQD 主实现
│   ├── ai题解/        # AI 辅助解答（Q01–Q10）
│   ├── Q06_SQD_vs_VQE/# VQE+SQD 混合方案
│   └── O01_最优ccsd_scale/ # λ 参数扫描
├── Optional1/         # ccsd_scale 深度研究（5 个实验）
├── sqd/               # 公共 SQD-EWF 工具库（15 模块）
├── ai_solutions/      # AI 生成的完整解答文档（HTML + Python）
├── lib/               # 共享库（可逆电路模块）
└── Problem1_Summary/  # Problem 1 总结
└── Problem2_Summary/  # Problem 2 总结
```

---

## 6. 关键发现总结

### 理论贡献

1. **CSS 编码态制备紧下界**（Problem 1.4d）：27 门最优线路，比通用方案压缩 16 倍 CNOT
2. **扇出门 Pareto 前沿**（Problem 1.6d）：最近邻约束下的不可兼得边界 {(7 CNOT, 7 深度), (10 CNOT, 6 深度)}
3. **SQD 覆盖条件**（Problem 2.5）：$S \gtrsim 3/p_{\text{double}}$，小 λ 下覆盖不足导致 LUCJ 退化为 HF
4. **EWF 线性标度**（Problem 2.7）：从指数级 $O[\binom{2N}{N_e}^3]$ 降至 $O(N)$
5. **木桶效应**（Problem 2.10）：大分子中 $\varepsilon_{\text{frag}}$ 主导 600–10000 倍，优化 SQD 无意义

### 工程贡献

1. **完整 SQD-EWF 工具链**：从分子构建到能量重构的一站式管道
2. **手写 CI 矩阵构建器**：独立于第三方库的 Slater-Condon 实现
3. **模拟器 ↔ 真机统一接口**：一键切换，安全默认
4. **NumPy 2.x 兼容**：全套兼容适配，零破产

### 数值发现

1. **最优 λ* = 0.30**（LiH，化学精度 1/7）
2. **多层 DF-LUCJ** λ ≥ 0.7 精确打到 FCI
3. **H₂ 周期性尖峰**：概率交换在大 λ 处产生窄能量尖峰
4. **结构性覆盖不足**：ansatz 限制 ≠ 采样不足，需从电路设计层面解决
5. **误差回升的多元机制**：噪声、参数化偏差、ansatz 表达力、采样涨落等共同作用

---

## 7. 项目文件索引

| 内容 | 路径 |
|------|------|
| Problem 1 完整解答 | `Problem1/Problem1-Solution.tex` |
| Problem 1 电路代码 | `Problem1/code/8_grover.py`, `problem7_reversible.py` |
| Problem 2 核心代码 | `Problem2/code/2_5_claude_lucj.py`（606行） |
| Problem 2 多层 DF-LUCJ | `Problem2/code/2.5-review2/problem_2_5_lucj_sqd.py`（735行） |
| SQD 公共库 | `sqd/common/`（15 模块） |
| VQE+SQD 混合 | `Problem2/Q06_SQD_vs_VQE/code/` |
| ccsd_scale 深度研究 | `Optional1/code/`（5 实验） |
| AI 辅助解答 | `ai_solutions/tencent_sparking_2026_solutions.html` |
| 共享电路库 | `lib/quantum_circuits.py` |

---

*本报告基于 `Tencent_Sparking_Program_2026(2).pdf` （腾讯量子实验室，20 页）与 `quantumnyloong` 项目代码库的系统审查生成。*
